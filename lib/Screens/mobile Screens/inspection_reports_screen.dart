import 'package:flutter/material.dart';

class InspectionReports extends StatelessWidget {
  const InspectionReports({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('data'),
      ),
    );
  }
}